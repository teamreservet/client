import './ticket.styles.scss';

const Ticket = ({
  ticketId,
  monumentName,
  childrenCount,
  indianCount,
  foreignerCount,
  amount,
  date,
  issuer
}) => (
  <div className='ticket'>
    <h1>ticket</h1>
  </div>
);

export default Ticket;
